1.95.0 — [[2026-09-25-lanes-survive-interruption]] + [[2026-09-25-media-load-standard]]: a portfolio lane's session ended mid-task and left ~295 uncommitted lines across six files, an untracked probe script, no commits, no progress file — the brief omitted `## Progress` though the lane was above line and nothing refused it; recovery was a fresh agent told to WIP-commit the leftovers first. Same day, operator: "media load is also something we need to codify as a standard practice. We never want media not loading." (1) `hooks/bin/agent-dispatch-gate.mjs` gains a 7th check: `checkComponentSystemProgress()` refuses a `Size: component`/`Size: system` dispatch whose prompt carries no `## Progress` heading naming a path — `line` stays exempt; new tests in `agent-dispatch-gate.test.mjs`. (2) `doer-rules.md` § You are the doer: a milestone is now a progress-file line **and** a local WIP commit on the lane branch, staging the lane's own touched paths by name — never `git add -A`/`commit -a` (how the stray probe script would have landed in the product tree either way) and never `git stash` (shared across worktrees); no uncommitted work survives past a milestone, the progress line names the WIP sha. (3) `skills/routing/SKILL.md` § Resume vs fresh + baton table: a lane found stopped or interrupted (session end, crash, a completion notification carrying a stopped status) is never resumed — the parent reads the worktree diff + progress file directly, dispatches a **fresh** `Agent` whose first instructed step commits any leftover WIP and re-verifies it as untrusted. (4) new `skills/media-loading` skill (model-invoked) — every media element visible on screen shows at least its still/poster from first paint through every motion (scroll/drag/hover/route change), video takes over when ready, never a blank or half-painted tile; method section (still/poster-first priority in page order, pre-sized variants per slot×DPR, entrance-never-before-decode, lookahead sized to the fastest motion, capped concurrent video decode, posters from the video's own first frame). Mechanical done-when: new `hooks/scripts/media-load-probe.mjs` (+ pure `lib/media-load-lib.mjs`, both with law tests, no browser launched in this repo's own suite — playwright resolved from the invoking repo's cwd, same pattern as `capture-website/scripts/capture.mjs`) drives Chromium and WebKit at a viewport/DPR under CPU (Chromium CDP) + network throttling, runs a named interaction (load/scroll/drag), prints the summed empty-visible-media count — the bar is 0; CPU throttling and true bandwidth shaping are Chromium-only, the WebKit number is a documented lower bound. Pointers only, not restated: `design-craft/references/TECHNICAL-DESIGN.md` new Law 11; `quality/SKILL.md` § Standing tech checks + checklist; `agents/reviewer.md` evidence bullet (the reviewer re-runs the probe, never reads a claimed 0); `dispatch-brief/SKILL.md` done-when clause; `routing/SKILL.md` work-type table row + `references/LIBRARIES.md` domain-library row. New law test `queue-1950-law.test.mjs` (18 assertions, one per outcome). Both rulings `encoded: 1.95.0`. Ceilings: `doer-rules.md` 246→256 lines (WIP-commit rule, no references/ dir to offload into); `routing` 1450→1560 words (Resume-vs-fresh paragraph + two baton/table rows); `agents/reviewer.md` 1450→1500 words (media-probe evidence bullet); `design-craft`, `quality`, `dispatch-brief` held at their existing ceilings (not tracked / offloaded to the new skill).

1.94.0 — operator queue row 130 (2026-09-24, "go"): nine items, each a rule line or a test. (1) `release-build.mjs` `parseUploadOutput()` only accepts a `.workers.dev`-suffixed URL as the preview URL — an earlier bare "first `https://` URL" match picked up an unrelated eslint doc link ahead of the real preview URL in the same stdout; new regression test with that captured shape. (2) `ds-regen.mjs`: `readGeneratedAt`/`readGeneratedAtFromExport` now match `design-system-handoff(-<stamp>)?.json`, not a literal `*design-system-handoff.json` suffix, so the plugin's stamped export filename (`…-handoff-2026-09-24-06-37-42.json`) is read instead of silently refused; `gh pr create` now always runs with an explicit `--head <branch>` and `cwd: worktreePath`, never the caller's own non-git cwd. New tests for both. (3) baton row (`skills/routing/SKILL.md` + `doer-rules.md` § Fixed evidence return): a doer's "already satisfied/true" on an operator-raised row is a claim — the parent reproduces it at the operator's own framing before relaying (`reproduce-lane-already-works-before-relay`). (4) an operator-set look value binds every consumer to one token in the dispatch slice (`skills/dispatch-brief/SKILL.md` + new `references/VALUE-CONSUMERS.md`); the reviewer's Spec axis treats a second declaration of an existing look as a red finding (`agents/reviewer.md`, `operator-values-are-site-wide`). (5) new `hooks/scripts/token-literal-diff.mjs` — a diff-scoped literal check: any new literal in `src/styles/**`/`*.css` equal to a value the consumed design system already generates a token for fails the lane's touched-file gates and the reviewer's, scoped to the diff only (pre-existing findings stay parked); rule lines in `skills/design-system/SKILL.md` § Conformance & review and `doer-rules.md` § Repo and safety; full test suite. (6) feel rows (input/scroll/hover speed) are a `prototype` lane first — live readout + URL knob, never a synthetic wheel, the shipped number set from the operator's own gesture (`skills/prototype/SKILL.md` rule 9, `skills/motion/references/BUILD.md`). (7) `lane-sweep.mjs` gains `isWaitLoopShell()` — sweeps a doer's own `until`/`while … pgrep … sleep` polling shell under the same safety fences (own-pid exclusion, 60s age floor) as the session-dir match; the next-server port sweep is bounded to 3220-3299 (was unbounded above 3220); new `lane-sweep.test.mjs`. (8) CI shape ruling [[2026-09-23-ci-shape]] encoded as a new `doer-rules.md` § CI (gate-or-doesn't-exist, one PR-only run, concurrency-cancel, install→typecheck→test→lint→build once) plus a pointer in `skills/release-deploy/SKILL.md` § The checklist; ruling's `encoded:` set to 1.94.0. (9) `model-routing` rewritten: `sonnet` is the default, not a ceiling — any tier, including the top one, may run as a child when the job shape calls for it, justified in `## Interrogated`; the "never the top tier as a child" line and the two-named-cases-only opus allowlist are removed everywhere (`skills/model-routing/SKILL.md`, `skills/dispatch-brief/SKILL.md`, `agents/reviewer.md`, `skills/dispatch-brief/references/INTERROGATE.md`); `queue-1870-law.test.mjs` and `queue-1850-law.test.mjs` updated to lock the new wording, superseding the 1.87.0 two-named-cases law. (10) `review-trails-the-operator-2026-09-20` ruling encoded whole, parent decision as item 10 of row 130: `agents/reviewer.md` § Round cap + `references/reviewer-preconditions-and-tier.md` gain a trailing-mode paragraph for look-judged work (single LIGHT review on the merged sha, background, round 2 stays mechanism-only); `doer-rules.md` § Repo and safety gains the look-judged gate-cadence rule (touched-per-iteration, suite once at merge, never gating); `agents/engineer.md`'s gate-cadence bullet points at it; `skills/dispatch-brief/SKILL.md`'s done-when states the merge step owns the suite; `skills/present-for-review/SKILL.md` states the operator's port serves the lane worktree, nothing waits on gates; `skills/routing/SKILL.md`'s baton row is rewritten look-judged: Engineer → Operator → merge → Reviewer (trailing), superseding the 1.84.0 `review-after-sign-off` wording (`queue-1840-law.test.mjs` updated to lock the new row, same spirit — operator first, reviewer off look judgment). Ruling's `encoded:` set to 1.94.0 (vault file only). Ceilings: `doer-rules.md` 215→245→246 lines (four new distinct rules plus one round-3 clause, no references/ dir to offload into); `dispatch-brief`, `routing`, `motion`, `agents/reviewer.md` held at their existing word ceilings by offloading elaboration to references/.

**Round 3 fix (same release, operator "go", one more fix, no third review).** `token-literal-diff.mjs`'s `declarationValuesInLine` terminator is now `;`, `}`, or end-of-line — was `;` only, so `.a{padding:12px}` (no trailing `;`) or a diff line still under construction (`--x: 12px`, no terminator yet) yielded zero declaration values and silently missed the literal. Six probes added: `--x: 12px` (no `;`) → 1, `.a{padding:12px}` → 1, `  padding: 12px` → 1, `a{padding:12px;margin:12px}` → 2, `--x: 12px;` → 1, selectors (`.gap-8 {`, `#brand-header-8px {`) → 0. `agents/engineer.md`'s Definition of done states the full suite runs once at merge for a look-judged lane, not on the handed-over sha; `doer-rules.md`'s prototype-lane bullet gains "no suite in bake/ladder briefs themselves." 1011/1011.

**Round 2 fixes (same release, reviewer round 1: 3 red, 2 amber).** RED — `token-literal-diff.mjs` rewritten: literal extraction is now scoped to CSS declaration VALUES only (text between `:` and `;`), never a selector or class name; numbers are word-boundaried (`18px` never misread as `8px`); a bare unitless number is skipped unless a token carries that exact unitless value and it isn't `0`/`1` (near-universal non-token CSS values — `flex: 1`, `opacity: 0`, `z-index: 1`, `line-height: 1`); 23 tests including the declaration-value/selector/unitless-0-1 shapes. AMBER — the default diff is now `git merge-base(HEAD, --base|origin/main|main)...HEAD` plus the working tree, concatenated, so a literal already committed on the branch is caught alongside anything still unstaged (new `defaultDiffText()`, 2 tests). AMBER — `lane-sweep.mjs`'s wait-loop sweep gains the same cross-session guard as the next-server/Playwright branches (ppid 1, or ≥30 min old, or the session dir named in argv) so a live operator keepalive loop with a young, non-1 parent is never matched (3 new tests). NOTE — `release-build.mjs` `parseUploadOutput`'s host match is now anchored (`\.workers\.dev` immediately followed by `/`, whitespace, or end of string), rejecting a lookalike host like `workers.devious.example.com` (2 new tests). 1005/1005.

1.93.2 — [[2026-09-22-scripts-not-agents]] § gh invocation: `gh --repo` only ever accepts `[HOST/]OWNER/REPO`, never a filesystem path — the first real `rulebook-sync.mjs` run failed with `gh pr create --repo /Users/.../jhd-design-system/main …` → `expected the "[HOST/]OWNER/REPO" format`, because `--repo` was carrying the local checkout path straight through. New `hooks/scripts/repo-layout.mjs` `ownerRepoFromUrl()`/`ownerRepoFromOrigin(gitDir)` — parses a checkout's own `origin` remote (https or ssh github.com URL) into `owner/repo`; `rulebook-sync.mjs` and `ds-regen.mjs` (the two scripts that were passing a path) now call every `gh pr create`/`checks`/`merge`/`list` with `--repo <owner>/<repo>` when `origin` resolves to github.com, or drop `--repo` and run `cwd`-scoped inside the checkout otherwise — never `--repo <path>`. `merge-after-review.mjs` and `lane-end.mjs` already split their own `--pr <owner/repo#n>` argument correctly and needed no change. **Idempotent re-run** — `rulebook-sync.mjs` now checks `git ls-remote --heads origin <branch>` before branching: a branch a prior (failed) run already pushed is fetched and reused rather than re-created with a duplicate `-b` (which used to abort with `a branch named … already exists`), and an already-open PR for that branch is reused rather than re-created with a duplicate `gh pr create`, so a run that died mid-way (e.g. on the old `--repo <path>` bug) can simply be re-run to completion. New `repo-layout.test.mjs` cases for `ownerRepoFromUrl`/`ownerRepoFromOrigin` (https, ssh, non-github, no-origin); `rulebook-sync.test.mjs`'s fake `gh` stub now rejects a path-shaped `--repo` the same way the real CLI does, and a new case proves the idempotent resume path end-to-end against a scratch remote. **Version-match gate + pin fix (same release, parent review)** — 1.93.1 shipped with `plugin.json` bumped to 1.93.1 but `.claude-plugin/marketplace.json` left at 1.93.0, turning seven `queue-18*0-law.test.mjs` "one matching semver" tests red on `main`; `queue-1930-law.test.mjs:113-123` compounded it by pinning the top CHANGED entry and `plugin.json`'s version to the exact string `"1.93.0"` (the pinning-gate shape every other queue law test avoids via "at or past"). `marketplace.json` bumped to 1.93.2 alongside `plugin.json`; the two 1930 pins rewritten to "CHANGED.txt carries a 1.93.0 entry naming scripts-not-agents + mechanical" (searched, not top-of-file) and "plugin.json is at or past 1.93.0". New commit-gate.mjs **version-match gate**: any commit that bumps `plugin.json`'s version now also reads `marketplace.json`'s indexed `plugins[0].version` and refuses the commit outright when the two don't match — mechanical, not prompt-trusted, so this class can't ship red again. `commit-gate.test.mjs` gains matching marketplace.json to its release fixture plus three new cases (mismatch denied, match passes, non-bump commit untouched).

1.93.1 — [[2026-09-22-scripts-not-agents]] § Layout: `rulebook-sync.mjs`, `release-build.mjs`, `ds-regen.mjs`, `merge-after-review.mjs` and `lane-end.mjs` now work against the house bare-repo layout (`<repo>/.bare` + `<repo>/main` worktree), not only a plain checkout — a 1.93.0 run of `rulebook-sync.mjs` failed with `git -C <repo> checkout -b … : not a git repository` because the script assumed a plain checkout and, given `<repo>/main`, would have switched the shared `main` worktree onto a branch. New shared `hooks/scripts/repo-layout.mjs` (`normalizeRepoRoot`, `isBareLayout`, `mainWorktreeOf`, `laneWorktreePath`): every branch-creating script now opens its own sibling worktree at `<repo>/<lane-name>` off `main` (never nested, never a `checkout -b`/`checkout <branch>` run inside `<repo>/main` itself), does its branch work there, and removes the worktree on success or failure, falling back to today's plain-checkout behaviour when no `.bare` is present. `ds-regen.mjs` gains the same treatment on **both** layouts (unified path) — it now commits on its own `chore/ds-regen-<version>` branch in a sibling worktree and leaves the PR open for review, rather than committing straight onto whatever `--repo` had checked out. A `--repos`/`--repo` entry naming the `main` worktree itself normalises to the repo root. New `repo-layout.test.mjs` (7 cases) plus bare-layout law tests added to each of the five scripts' existing test files (happy path, `/main`-normalisation, and mid-run-failure cleanup), all plain-checkout tests kept green unchanged.

1.93.0 — [[2026-09-22-scripts-not-agents]]: mechanical lanes become scripts, plus the same-day mechanical-first principle (operator, 2026-09-22: *"is there anything else we use agents for that could just be a script?"* → *"let's do it"*; same day: *"i have preference for … ways to do things that takes the need away from using ai … where we can go mechanical or use things like scripts that a preference"* / *"this is also an approach to solutions that i would expect you and doers to do."*). Four mechanical lane shapes are now deterministic plugin scripts under `hooks/scripts/`, each with `--dry-run` and a law test against a scratch git repo (never a real remote), `gh`/`pnpm` stubbed on `PATH`: **`release-build.mjs --repo <path> [--sha <sha>]`** (worktree → install → typecheck → build → upload → prints version id + preview URL → removes the worktree; replaces the haiku "build main to a preview version" lane); **`rulebook-sync.mjs --source <discipline main> --repos <list>`** (per repo: sync `doer-rules.md` into `.cursor/rules/doer-rules.mdc`, skip if identical, branch/commit/push/PR/CI-wait/squash-merge/ff-main, prints a table; replaces the five-repo sync lane); **`ds-regen.mjs --export <zip|dir> --repo <DS path>`** (vendors under `design/handoff/v<N>-<date>/`, repoints `latest`, runs tokens/tokens:check/test/typecheck, commits in the #68 shape, prints the export's `changes` block + VALUE-DRIFT count — the parent still rules on the count); **`merge-after-review.mjs --pr <owner/repo#n> --repo <path> [--then-build]`** (refuses unless every check is SUCCESS and `mergeable`, otherwise squash-merges/deletes-branch/ff-mains/prunes; replaces the parent's manual merge chain). `lane-end.mjs` gains `--then-merge <owner/repo#n> --repo <path> [--then-build]`, chaining script 4 then script 1 after a landed lane's queue-row write, per the ruling's Boundaries paragraph. `routing`'s baton table points at the four scripts via new `references/MECHANICAL-SCRIPTS.md` (ceiling raised 1400→1450 for the pointer row). **Mechanical-first** — a deterministic mechanism (script, token, data-driven rule, pinned test, existing tool) comes before a model call, a manual step or a judgement call; a brief or design that reaches for AI where a script would do is a finding. Encoded as the first bullet of `doer-rules.md` § You are the doer, a fourth non-negotiable in `skills/quality/SKILL.md`, and a new rung in `code-minimalism`'s ladder (renumbered 7→8 for "write the minimum working code"). New law test `queue-1930-law.test.mjs`; new script pairs `release-build.mjs`/`.test.mjs`, `rulebook-sync.mjs`/`.test.mjs`, `ds-regen.mjs`/`.test.mjs`, `merge-after-review.mjs`/`.test.mjs`.

1.92.1 — [[2026-09-22-spend-levers]] rules 1-3 plus rule 5 (operator, 2026-09-22: *"spend is getting a bit high"*; rule 4 — model choice per orchestration session — was withdrawn same day, *"this wouldn't match fable as the planner though i don't think"*, and stays unencoded). **Rule 1** — read-back only above `line`: `doer-rules.md` § Size class states a `line` brief never asks for a read-back stop, and `agent-dispatch-gate.mjs` gains a sixth check (`checkLineNoReadBack`) refusing a `Size: line` dispatch whose prompt instructs a `## Read-back` return or a `next: parent (go?)` stop; `dispatch-brief/SKILL.md` names the same refusal. **Rule 2** — the evidence return is the ledger plus gate tails, full stop: `doer-rules.md` § Fixed evidence return states narrative, reproduction logs and per-frame readings go to the progress file, linked by path, never pasted into the return; `dispatch-brief/SKILL.md` repeats it. **Rule 3** — the parent's reply to a read-back is one line, `go` or one correction, never a restatement: `doer-rules.md` § You are the doer and `dispatch-brief/SKILL.md` both carry it. **Rule 5** (same-day addendum: the orchestrator "avoids mechanical grunt work") — new `hooks/scripts/lane-end.mjs`, one deterministic parent call per completion notification: sweeps (`lane-sweep.mjs`), replaces the named queue row in the operator's own section (refusing an absent row or a cross-section write), greps the write back (`queue-write-check.mjs`), commits + pushes the queue file and evidence dir only, and prints PR CI status when named; `routing`'s baton table names it as the lane-landed parent call, replacing the bare `lane-sweep.mjs` call. **Round 2 fix (same day)** — `findRow`/`replaceRow` searched the whole queue file first-match, so a row number shared by two sections (e.g. row 9 in both Portfolio and Discipline) could falsely refuse a real `--section` match or, with no `--section`, silently overwrite the other project's row; `--section` is now required (no whole-file fallback) and the lookup/replace is scoped to that section's own line range only, proven by a two-section row-9 fixture in both `lane-end.test.mjs` and `queue-1921-law.test.mjs`. Added `--dry-run` (prints the replacement and the commit message it would make; no write/commit/push/sweep/PR call). New law tests `queue-1921-law.test.mjs`, `lane-end.test.mjs`.

1.92.0 — [[2026-09-22-lessons-for-1-92]] + [[hoverboard-rounds-14-15-lessons-2026-09-21]] (items 1, 3, 5, 6): source-contract gate, house rules block, frame-first proof, runtime state proof, lane clock, motion proof, queue/deploy mechanics (operator, 2026-09-22: *"i'd like to actually discuss these and get them actioned"* / *"go"*, with three same-day adjustments — the lock-row gate also refuses a backticked mechanism identifier in the quote cell; the skills-named gate applies to every persona, not build verbs only; the portfolio `CLAUDE.md` DS-path fix ships separately, PR 149, and is not re-touched here). **Change 1** — a Source-contract lock row is `export-silent` | `export-vs-ruling` | `operator-round` only, quote plus source, never a parent-authored `Means technically`; banned in the quote cell whatever the source: a mechanism, a function/token/easing name, a value, or a "recommended" answer promoted to a rule. `hooks/bin/agent-dispatch-gate.mjs` gains two new checks (skills named, on every persona; Source-contract lock-row shape) alongside the three from 1.79.0; `dispatch-brief/SKILL.md` points at the new `references/SOURCE-CONTRACT-LOCKS.md`. **Change 1 additions** — a fixed **House rules** block (`references/HOUSE-RULES.md`: design-system path, motion-law path, markup standard, layout policy, component law, contract order, pointers to `fleet/rulings/token-rulings.md`) is copied whole into every brief above `line`; the skills-named gate check is mechanical, not prompt-trusted. **Change 2** — frame-first proof: `handoff-to-code/references/coverage-ledger.md` requires the export-frame PNG beside the build at the frame's width, next to the per-node ledger, before any link; `agents/reviewer.md` checks the ledger against the export JSON's node list before approving. **Change 3** — a prior slice's `## State` claim is reproduced on the running build, never verified by reading the code (`doer-rules.md`, `dispatch-brief` § State). **Change 4** — the progress file's first line stamps the dispatch time; silent 15 minutes reads the worktree diff, silent 30 stops the lane (`doer-rules.md`, `routing`'s baton table gains the 15-minute row alongside 30). **Change 5** — motion review standard 14: any enter/exit claim is proven by a sampled property-over-time trace on the running build; a stylesheet-text assertion is a note, not proof (`skills/motion/references/REVIEW.md`). **Change 6** — new `hooks/scripts/queue-write-check.mjs` greps a queue-row edit back and fails loud (`vault-write/SKILL.md`); `release-deploy/SKILL.md` and `present-for-review/SKILL.md` name the standalone parent promote (`wrangler versions deploy` and siblings run as their own Bash call, never chained after a merge, never a verb inside a brief). **Parent ledger-block ruling answered in the same release** — the commit-gate's lesson-ledger check refused on `hoverboard-rounds-14-15-lessons-2026-09-21.md`; its items 1, 3, 5 and 6 are encoded here (item 1: `doer-rules.md` requires a load/timing number to count only on the deployed link and the operator's device; item 3: `doer-rules.md` + `routing` require a full suite over the tool's foreground timeout to run in its own gate-run lane, never the parent shell; item 5: `dispatch-brief`'s read-back line states which object a dimension sizes; item 6: folded into Change 6 — a queue row named in chat is written to the file in the same tool call), with the lesson's own `## Encoding` section noting items 2 and 4 as hoverboard-rig-specific, not a general plugin rule. `review-trails-the-operator-2026-09-20` stays queued — a ruling scoped by CHANGED links, not named here. Ceilings raised: `dispatch-brief` 1350→1450 words / 16→18 checklist items, `routing` 1350→1400, `agents/reviewer.md` 1400→1450. New law test `queue-1920-law.test.mjs`; two new fixtures + eleven new cases in `agent-dispatch-gate.test.mjs`.

1.91.0 — [[2026-09-21-eight-class-ledger]] + [[2026-09-21-never-remove-a-worktree-by-pattern]]: the coverage ledger has eight row classes; reviewer sampling is stratified across classes; worktree removal is fenced (operator, 2026-09-21: *"i'm trying to understand why so much stuff is getting missed or not updated and if our updates to the plugin will fix that problem"* · *"is there anything else we're missing?"* → *"yes to both"* — cause on record: the nav rebuild's "complete" ledgers covered geometry/tokens only; misses on tokens-per-mode, copy, behaviour and absence reached the live site) — `qa-acceptance/SKILL.md` § The coverage ledger names the eight row classes (geometry-and-placement per device; tokens per mode with a new `mode` column; copy export-vs-built per route; links/targets; states/variants/prototype flows; behaviour annotations with a proving test/probe; semantics/a11y; absence — retired lock rows + removed nodes), each present or "none in scope." `handoff-to-code/references/coverage-ledger.md` specialises each class for an export contract (copy from the block export AND the layout examples per route; absence sourced from the changelog/delta note). `agents/reviewer.md`'s spot check now samples across classes, never several rows from one, and re-proves absence rows by grep. **The worktree fence** (lesson `never-remove-a-worktree-by-pattern`: a pattern-matched `git worktree remove --force` deleted four `main` trees across bare-layout repos) — `doer-rules.md` § Repo and safety gains a `Worktrees (bare-layout repos)` rule: `git worktree add` for mechanical/sync lanes, a branch checked out in `main` is red, `git worktree remove` only by an explicit path verified under `worktrees/`, `main` never a target. New `hooks/scripts/lane-sweep.mjs --worktrees <repo>` mode: removes only worktrees under `<repo>/worktrees/` whose branch is merged into `origin/main`, refusing any path outside `worktrees/`. `skills/wrap/references/CLOSING-CHECKS.md` § Repo topology names the fence. Ceiling raised: `agents/reviewer.md` 1350→1400 words. New law test `queue-1910-law.test.mjs`.

1.90.0 — [[2026-09-21-lane-end-sweep]]: lane-end sweep is a script; doers never poll (operator, 2026-09-21, on orphaned `until … sleep` loops from finished lanes still listed under background tasks: *"can we do something to make sure they just [don't] stick around moving forward"*) — `doer-rules.md` § You are the doer gains a no `until`/`while … sleep` polling loops and no detached-shells rule, echoed in `agents/engineer.md` / `agents/ux-designer.md` / `agents/researcher.md` / `agents/reviewer.md`. New `hooks/scripts/lane-sweep.mjs`, run by the parent on every completion notification: stops `next start`/`next-server`/`next dev` processes listening on a TCP port >= 3220 (resolved via `lsof`, never 3210/3211, never one not listening), Playwright/headless Chromium, and any shell whose argv references the session's task-output directory — one line per process printed (pid, age, port, argv, action), exit 0 always, nothing else ever killed. `routing`'s baton row and hard-rule sentence, plus `output-styles/discipline.md`'s completion-notification step, now name the script, replacing the by-hand sweep sentence from 1.89.0. New law test `queue-1900-law.test.mjs`, including unit tests of the port fence against a fake process table.

1.89.0 — [[2026-09-21-proportionality]]: the change's size class fixes the process (operator, 2026-09-21: *"i'm not sure a cap is really solving the problem... i just really don't understand why anything should be taking this amount of time"* · on screenshots: *"a quick confirmation at the size of the figma designs should be pretty quick to see any issues"* → *"that sounds good"*) — every brief names **line** (a value/selector/guard/copy string: one file, only the covering tests, no build/upload/screenshots/ledger), **component** (one component set/contract unit: coverage ledger, covering tests, one screenshot per Figma sample width 375/768/1280/1920 checked at a glance, one upload per batch), or **system** (cross-component/pipeline/release: today's full process); the full suite/build/deploy checks run once per PR in CI, never inside a lane; lane verification is one deterministic repro plus three reps, a heavier sweep its own lane; no background runs inside a lane, and a lane stops only its own pids. New `## Size class` block in `doer-rules.md`; `dispatch-brief`'s `Size:` field + checklist row + verification-rule sentence; `INTERROGATE.md`'s ninth question; `routing`'s CI-once/parent-sweep hard rule + baton row; `present-for-review`'s scoped pixel proof; `qa-acceptance`'s ledger-depth-per-class line. **Two riding ambers closed**: the read-back and progress-file exemptions in `doer-rules.md` now both read **line** (unified, replacing the old "trivial/small-fix" vs "trivial" mismatch); `agents/reviewer.md`'s progress-file line moved out of the fail-conditions list to its own first-step bullet. Ceilings raised: `dispatch-brief` 1250→1350 words, checklist 15→16 items. New law test `queue-1890-law.test.mjs` (17)

1.88.0 — [[2026-09-21-lane-progress-file]]: lane progress is a file, not a stop (operator, 2026-09-21: *"can we update discipline so progress isn't so blind?"* → *"Yes"*) — every lane above trivial keeps a progress file at the brief's `## Progress` path (default `<lane evidence dir>/progress.md`), appending one timestamped line at each of six fixed milestones (read-back returned · red test written · cause found · green · gates green · uploaded/pushed); milestones never replace the fixed evidence return; no new milestone for 30 minutes stops the lane and re-briefs a fresh agent from the last recorded milestone. Encoded in `doer-rules.md` § You are the doer (after the read-back step), `dispatch-brief`'s four-part brief + checklist, `routing`'s baton table, `output-styles/discipline.md`'s "How are we looking?" answer, and the first-step lines of `agents/engineer.md` / `agents/ux-designer.md` / `agents/researcher.md` / `agents/reviewer.md`. **The release gate, scoped to the rulings a release names** — `hooks/scripts/lesson-ledger.mjs` refused a version bump while ANY `fleet/rulings/*.md` in the vault was `queued`, including rulings from unrelated sessions; the gate (`hooks/bin/commit-gate.mjs`) now blocks only on a queued ruling CHANGED.txt's own top entry names (`[[stem]]` or a `fleet/rulings/<stem>.md` path) — every other queued ruling is a warning in the gate's output, never a refusal; a queued lesson is unaffected and still blocks every release. `lintLessonLedger`'s unscoped default is unchanged; scoping is additive via a new `namedRulings` option, plus new exports `namedRulingsFromChangedEntry` and `topChangedEntry`. New law test `queue-1880-law.test.mjs` (13)

1.87.0 — two operator rulings encoded: **sonnet is the default ceiling** (`sonnet-default-ceiling`, 2026-09-21) — `sonnet` is the default for every dispatched doer of every job shape, `haiku` for mechanical work, `opus` only with a written justification in `## Interrogated` naming one of two cases (adversarial review of a change with fleet-wide blast radius, or novel architecture with no contract to point at); `model-routing`'s Claude model map and escalation rule replace the old "Design/UX taste → opus" and "Hard architecture → opus — justify" cells with the two named cases; `dispatch-brief` § Persona + model, `agents/reviewer.md` (own default stays sonnet), `INTERROGATE.md` (question 8, folded into the eight-question set) and `output-styles/discipline.md` carry the same ceiling. **The doer read-back** (2026-09-21, operator: *"would it make sense to also introduce brief reading and playback with any question from the doer before commencing... cheap to get the briefs clear, expensive to iterate or redo work"*) — every lane above trivial/small-fix returns `## Read-back` (contract items enumerated, assumptions, questions) and stops at `next: parent (go?)` before any edit; the parent's answer continues the same agent in the same context, the one non-red resume allowed. Encoded in `doer-rules.md` § You are the doer, `agents/engineer.md` / `agents/ux-designer.md` / `agents/researcher.md` first-step lines, `dispatch-brief` done-when, `routing` § Resume vs fresh + baton table. Ceilings raised where the growth was real:
`dispatch-brief` 1100→1200 words, `routing` 1200→1250 words, `agents/reviewer.md`
1250→1300 words. New law test `queue-1870-law.test.mjs` (11); 719/719

1.86.0 — the brief is the lever, not the model (operator ruling `brief-is-the-lever`, 2026-09-20, with the same-day refinement): **brief interrogation** — before any dispatch above trivial/small-fix, the brief is read as the doer would and answered against a fixed seven-question set (can every contract item be enumerated from the brief and its pointers alone; which numbers have no source or knob; which mechanisms are unnamed; where would the doer be forced to assume; is the done-when measurable at the operator's framing; is there exactly one contract unit; which named skills are missing for the domain) — run by a cheap read-only agent (`haiku`, findings only) or the parent inline for trivial/small-fix; findings fix the brief before the lane opens; the result (or "inline, clear") is recorded in the brief under `## Interrogated`. Question set and interrogator template live once in `dispatch-brief/references/INTERROGATE.md`, pointed at from `dispatch-brief` § Interrogate the brief, its fourth-part field and its checklist (13→14 items; ceiling held at 1100 words, paid for by trims elsewhere in the file, not raised). `model-routing`'s escalation rule gains the precondition: a failed lane on an un-interrogated brief is a brief defect, not a model defect — fix the brief and re-run the same tier before escalating. `routing` § Load order gains step 5 between the brief and `Agent`; `output-styles/discipline.md` § Announce by doing names the same step. Ceiling raised where the growth was real: `routing` 1150→1200 words. New law test `queue-1860-law.test.mjs` (9)

1.85.0 — accuracy in build, before the link (operator ruling `accuracy-before-the-link`, 2026-09-20, with two addenda): the **coverage ledger** replaces the deviation table and generalises to every contract — one row per contract item (`item · source ref · file:line · measured value · status`) written before the work, a missing row red, deviation a status value, variance never collapsed into one literal; the rule and row shape live once in `qa-acceptance` § The coverage ledger, with `handoff-to-code` (house overlay `references/coverage-ledger.md`: node ids, per-device/per-page modes, the page × state × device composition table derived before build), `capture-figma`, `capture-website` and `capture-motion-source` naming only what an item is. **One contract unit per lane, always** — a brief covering more than one export `COMPONENT_SET`, captured page or component is malformed at any model, never a reason to escalate (`dispatch-brief` + `references/ACCURACY.md`, `model-routing`). **Pixel proof at the operator's framing** — a headed screenshot at the operator's viewport and each breakpoint family with a pixel assertion on the region built, never a computed-style read; it gates the parent, not the link's speed (`present-for-review` § Before the link goes out, `doer-rules.md`, `dispatch-brief` done-when, `agents/engineer.md`, `agents/ux-designer.md`). Cause on record: the nav rebuild reached the operator three times with a footer that never painted and per-device/per-page export values collapsed into single literals. `skills/handoff-to-code/SKILL.md` is untouched — it stays a byte mirror of handoff-css, so the house law sits in its `references/` overlay. Ceilings raised where the growth was real: `dispatch-brief` 1000→1100 words / 11→13 checklist items, `capture-figma` 1200→1250. Review round 2: the layout-example export pair is a named required input to every component-set lane (it travels as context, never a second contract unit), ledger scope stops at an `◆instance of` boundary, the link waits on the two proofs and is never a review round, `qa-acceptance`'s description fires on the ledger, and the engineer's baton follows `doer-rules.md` row 5 (`next: operator` for UI). New law test `queue-1850-law.test.mjs` (11)

1.84.0 — two operator rulings encoded whole: review-after-sign-off (look-lane reviewer dispatched only after the operator's yes, never concurrent — `routing` § Baton, `present-for-review`, `agents/reviewer.md` preconditions, `output-styles/discipline.md` § The bar, `doer-rules.md` engineer-return row) and brief-carries-operator-words (the operator's words and images are the contract, verbatim; no invented numbers; outcomes and their measurement, never mechanisms; mandatory read-back on any visual note; parent additions marked "parent:" — `dispatch-brief` one rule + checklist + `references/BRIEF-WORDS.md`, `prompt-craft`, `grilling`, `agents/reviewer.md` unsourced-target flag, vault `neutral-briefs-never-steer.md` cross-reference). Ceilings raised where the growth was real: `dispatch-brief` 900→1000 words / 8→11 checklist items, `agents/reviewer.md` 1200→1250 words. New law test `queue-1840-law.test.mjs` (10); retro-applied `queue-1830-law.test.mjs`'s version pin to "at or past 1.83.0"; 690/690 (2026-09-19)

1.81.0 — the 2026-09-13/16 queue encoded: lean lane cadence as fleet law (one review round, cap 2, round 2 red-only, amber rides the next change — `agents/reviewer.md` § Round cap; touched gates per row + one full suite per lane, never concurrent, prototypes exempt — `doer-rules.md`; invariants and framing-checked measurements in `dispatch-brief`; the parent views a disclosed visual gap before presenting); the operator queue printed in full under "Needed from you" on every status (`output-styles/discipline.md`, `wrap`, `pause-resume`, `present-for-review`, `routing`); the operator is the cheapest visual gate (visual lanes return the link, not renders); one worktree name per lane; border tokens paint at whole CSS pixels (`design-system`, `audit-build`); one implementation per component and spacers are margins (`design-system`, `markup-standard`, reviewer Structure check); semantics are a build standard, never authored in Figma (`markup-standard`, `capture-figma`); export values win a drift (`agents/engineer.md`); merge briefs state the review record (`release-deploy`, `releaseops`, routing baton); Figma plugin fixes land in `figma-plugins/main/handoff` (`capture-figma`, routing LIBRARIES); two `capture-motion.mjs` browser-snippet bugs fixed with a parsing test; handoff-to-code re-mirrored from handoff-css 0.7.2. New law tests `queue-1810-law.test.mjs` (17) + `capture-motion-script.test.mjs` (5); 662/662 (2026-09-16)

1.80.0 — six round-time laws from the hoverboard-session inventory: gate tiering (per-commit typecheck+touched tests, full suite once before `next: reviewer`/CI, 3-pass diagnosing cap, `skills/quality/SKILL.md`); never end a turn waiting on a background gate; engineer's own lock-row count before `next: reviewer`; never kill/restart/reuse a process or port you did not start; a `SendMessage` from the parent session is a legitimate continuation of the brief, not content-injected instructions; a build-identity proof runs each arm in its own scratch tree with a real package install, reproduces the compared on-disk shape, compares the shipped bundle by byte first, and commits its harness (`agents/reviewer.md` § Evidence, encodes `proof-harness-real-install-2026-09-12`). Six new `law-single-source.test.mjs` assertions (33 total in that file); 625/625 (2026-09-12)

1.79.2 — handoff-to-code mirror from handoff-css 0.4.3 (schema v11 explicit alignment, skill alignment law) (2026-09-12)

1.79.1 — handoff-to-code mirror from handoff-css 0.4.1 (v9 hints), Build-standards law test decoupled from table-vs-list formatting (2026-09-12)

1.79.0 — dead references removed, one home per law, ceilings with reference offload (2026-09-12)

Foundations hardening, W4. Four dispatches on one branch.

**Dead references.** `design-taste-frontend` (README, `design-craft`, `routing`) and
`impeccable` (README, `design-review`) were named in shipped docs but ship no folder — an
agent reaching for either finds nothing and improvises. Both removed at every site; routing's
greenfield row went with the first, since that lane has no skill behind it. All 11 skill
folders no routing table named were traced: every one is live and chain-loaded by a sibling,
so none was retired and each is now routed. `hooks/scripts/skill-reference-integrity.test.mjs`
is the standing guard — every `skills/<name>` path and sibling link resolves, the retired
names never reappear, and no skill folder goes unrouted.

**One home per law.** Five laws had up to eight copies each. Homes: `doer-rules.md` for the
`Agent` prohibition, the Open-gaps wording and the doer port range; `output-styles/discipline.md`
for operator-facing status; `agents/reviewer.md` § Round cap for the review round cap. Every
other site is a one-line pointer naming file and section — no paraphrase left to drift.
`hooks/scripts/law-single-source.test.mjs` pins each law three ways: verbatim at its home,
absent from every other shipped doc, pointer present at each former copy site.

**Ceilings, met by offload.** Eleven files slimmed, nothing deleted — 24 new `references/*.md`
carry the moved material, and `agents/references/` is new so an agent charter ceilings the
same way a skill does:

  capture-figma   9,578 -> 1,200      routing         3,254 -> 1,103 (ceiling 1,150)
  motion          5,470 ->   978      wrap            3,115 ->   795
  reviewer        2,017 -> 1,199      markup-standard 2,778 -> 1,024
  vault-write     2,394 ->   641      banana          2,273 ->   567
  nextjs          2,082 -> 1,140      prompt-craft    1,811 ->   633
  issue-triage    1,693 ->   638

`hooks/scripts/skill-ceilings.test.mjs` holds each ceiling on the BODY, requires a
`references/` directory carrying real weight (a ceiling met by deletion fails), checks every
`references/` link resolves, and records each file's frontmatter word count so the trigger
surface cannot be raided to pay for the body. `handoff-to-code` is untouched: it is a byte
mirror of handoff-css's `dist/claude-plugin/SKILL.md` and slims at its source repo.

**Agent dispatch gate.** `hooks/bin/agent-dispatch-gate.mjs`, PreToolUse on `Agent|Task`,
denies a dispatch naming the failing item: description must read `cloud — persona (model):
task` (routing rule 9 + model-routing), `model` must be set explicitly, and the prompt must be
under 600 words with markdown table rows excluded — counting a locked table would push the
parent to slice the lock to fit, the malformed brief rule 10 forbids. `Explore` and `Plan` are
exempt as parent reconnaissance. Three laws that were prompt-trusted since 1.74.0 are now
mechanical.

**Cursor retired** (operator ruling). `cursor-corpus-drift.test.mjs` removed along with every
mention of Cursor as a surface, the `discipline-cursor` sibling and the `sync-discipline-*`
scripts. `.cursor/rules/` stays — that path is where the always-on layer lives in a product
repo regardless of editor, and AGENTS.md and `sync-doer-rules.mjs` now each say so in one line
so the next reader does not read it as an editor dependency.

**Fleet scripts rehomed (partly).** `scripts/jhd-container-clone.sh` moved into this repo —
body byte-identical, only a provenance header added; it is pure git/rsync with no payload or
editor dependency, so `new-product` now names a real path instead of a placeholder.
`sync-discipline-into-product.sh` did NOT move: it is inert without the `rules/*.mdc` +
`templates/*/` payload it copies, and that payload is the Cursor-era always-on corpus this
release just retired — importing it would have un-retired Cursor and restored a second,
competing routing law one commit after single-sourcing the first.
`hooks/scripts/fleet-scripts.test.mjs` asserts every command path `new-product` names exists,
that no placeholder survives, and that the skill does not hand a doer an overlay command it
cannot run. The overlay payload's home is an open decision.

**Evidence-archive rule.** Written into `present-for-review` and `wrap` at the moments evidence
piles up: evidence and probes never live in a product repo; a round's evidence goes to
`~/JHD/vault/main/estate/captures/<product>-evidence/` with a manifest row via
`scripts/evidence-archive.mjs`.

`handoff-to-code-mirror.test.mjs` reconciled: byte-identical to the 1.78.0 version, so there
was no regression — the "0 tests" reading came from the absent-root path, which registered one
skipped test and reported `pass 0` while the suite stayed green. The plugin-side assertions now
run unconditionally; only the cross-repo byte-compare stays conditional on the sibling checkout.

Suite: 601 checks green (`node --test hooks/scripts/`).


1.78.0 — doer-rules sync, corpus drift test, mirror guard (2026-09-12)

`hooks/scripts/sync-doer-rules.mjs` writes `doer-rules.md` into a target product repo's
`.cursor/rules/doer-rules.mdc` (frontmatter + verbatim body), closing the gap AGENTS.md
had promised since 1.77.0; run for portfolio and jhd-design-system (each committed on
`chore/doer-rules-1.78.0`, not pushed). `hooks/scripts/cursor-corpus-drift.test.mjs`
compares the plugin's `skills/` against `discipline-cursor`'s corpus skill-by-skill —
today it reports (non-fatal by default, `DISCIPLINE_MIRROR_STRICT=1` to gate) 4
skill-set mismatches and 25 drifted `SKILL.md` files; the corpus itself is not touched in
this release. `hooks/scripts/handoff-to-code-mirror.test.mjs` (shipped 1.75.0) verified
already correct — byte-compares the plugin's `skills/handoff-to-code/SKILL.md` against
handoff-css's `dist/claude-plugin` copy and fails on drift.

1.77.0 — dispatch-brief rewritten to four parts; doer-rules.md is tier 1 (2026-09-11)

Two tiers replace one 4,612-word skill. Tier 1 `doer-rules.md` (repo root) carries the
standing law every dispatched agent reads whole: you are the doer, repo and safety, the
Fixed evidence return, ports, notes ledger. Tier 2 `skills/dispatch-brief/SKILL.md` is
under 900 words — one rule (the brief points at the contract, never restates it), an
eight-scenario contract table, Goal / Context / Constraints / Done-when, and one
eight-item list before dispatch. Vehicle + work-type skill tables moved to `routing`;
neutral wording and the mechanism-named rule moved to `prompt-craft`. New
`brief-shape-law.test.mjs` pins the ceilings and the counts.

1.76.0 — status-is-done-or-not-done and retarget-is-a-continuation-slice encoded (2026-09-11)

Encodes `fleet/rulings/status-is-done-or-not-done-2026-09-11.md` (operator: "we're either
done or not done" — never a third "blocking" state) and
`fleet/lessons/retarget-is-a-continuation-slice-2026-09-11.md` (a lock widened mid-flight,
doer unreachable, is delivered as a continuation slice on the same branch, not a wait or a
re-dispatch). `output-styles/discipline.md`'s "Say it so it lands" gains the "Status is done
or not done" law paragraph and a "Third state" entry in the ways-this-has-gone-wrong list;
`skills/dispatch-brief/SKILL.md`'s "Fixed evidence return" bans "nothing blocking" from the
`Open gaps` field (mirrored in `agents/engineer.md` and `agents/reviewer.md`), and its "Caps
and continuity" section states the continuation-slice trigger. New law-test block in
`hooks/scripts/source-contract-law.test.mjs` asserts all three sentences verbatim plus a
negative sweep: no operator-facing file still says "nothing blocking" outside the one
banned-word citation. Both vault notes set `encoded: 1.76.0`.

1.75.0 — handoff-to-code is generated from handoff-css@7f51638; mirror drift test; pnpm file: lesson encoded (2026-09-11)

`skills/handoff-to-code/SKILL.md` is no longer hand-maintained in this repo — it is a byte
copy of handoff-css's own `skills/handoff-to-code/dist/claude-plugin/SKILL.md`, taken at
`main` sha `7f51638a3123ca79b5cc9aca0b5b7939350c80db` (handoff-css `v0.3.0`, merged
2026-09-11: export contract P14, one-skill-every-host Lock 2, `conform` P16, the P15
rounding rule, and schema 9). `hooks/scripts/source-contract-law.test.mjs` confirms every
ratified sentence still survives in the copied file. New
`hooks/scripts/handoff-to-code-mirror.test.mjs` guards future drift: with
`$HANDOFF_CSS_ROOT` (default `~/JHD/handoff-css/main`) present, it byte-compares the
plugin's copy against the package's dist file and asserts every entry in the package's own
`test/skill-law-sentences.mjs` survives; absent, it skips with the reason printed, or fails
under `DISCIPLINE_MIRROR_STRICT=1`.

Encodes `fleet/lessons/pnpm-file-deps-are-copied-2026-09-11.md` (the portfolio design-system
deploy that shipped a stale token because `pnpm install` without `--force` is a no-op on a
`file:` sibling): `skills/dispatch-brief/SKILL.md`'s "Deterministic gates precede review"
paragraph and pre-dispatch checklist, and `skills/release-deploy/SKILL.md` §1 Build plus
`skills/release-deploy/references/DEPLOY-CHECKLIST.md` §1, now all state the rule —
`pnpm install --force` and an installed-copy identity assert precede any gate or deploy for
a `file:` sibling consumer. `source-contract-law.test.mjs` gains block E asserting both
sentences verbatim.

1.74.0 — handoff-to-code: the export pair is the contract (2026-09-10)

New skill `skills/handoff-to-code/SKILL.md`. Where 1.73.0 made the brief point at a
Design Handoff export instead of re-describing it, 1.74.0 gives the doer the procedure:
**the handoff pair is the contract — every node, token, copy string and annotation in it
is built, never Figma pixels, never inferred values.** Six steps: reconcile the
markdown's companion `state`/`content` hashes against the JSON's
`fingerprint.designSystemStateHash`/`contentHash` and its `schemaVersion` against the
consumer's declared versions (a mismatch is a stale-pair defect, not a build); run
`handoff-css --config … --check` and read every warning in the report's
`Responsive classes (P11) and aliases (P12)` section — `VIEWPORT_UNFLAGGED`,
`VIEWPORT_OUTSIDE_GROUPS`, `VIEWPORT_CLASS_WITHOUT_FRACTION`,
`VIEWPORT_FRACTION_DISAGREES`, `BUILD_CELL_CONTRADICTORY` — building *through* a warning,
never around it; map every `$variable` through its **`codeSyntax.WEB`** name, where no
emitted token is a defect and a legacy alias is not the name; implement with the
handoff's **Build standards 1–5** as the mechanism rules (a `[Grid]` frame is
`display: grid` and `col-span(N/M)` is `grid-column: span N` — column maths is a defect
even when it measures right); build the Content Outline verbatim, with `⚠ placeholder` a
defect rather than a guess and the newest pair beating any prior record; return the
deviation table `element · Figma binding · token used · value · status · ruling ref` and
**stop for a ruling on drift**. Worked example is the real 2026-09-10 nav footer
(`$device/screen-height/full` → `height: var(--device-screen-height-full)`).

`capture-figma` hands over: `## Export shape` and the values pass now point at
`handoff-to-code` when a pair exists; it keeps the read-order law, the lanes, the copy
and template-layout lanes, and recapture. `dispatch-brief`'s Source contract and
`agents/engineer.md`'s stack name the skill as the doer's, and the engineer's evidence
return names the per-node form of the deviation table.
`hooks/scripts/source-contract-law.test.mjs` gains 9 assertions on the new ratified
sentences, including that the skill reads standalone — no machine-local paths, house
names only under `## At JHD` — because the same skill ships to Figma Community inside
the `handoff-css` package (`skills/handoff-to-code/SKILL.md` there).

1.73.0 — the export is the contract; reviewer structure check; State section; lesson ledger (2026-09-10)

Five root problems from the 2026-09-09 nav rejection ship one release each with an
enforcing test. **A1 — the source is the contract:** `skills/dispatch-brief/SKILL.md`
gains `## Source contract (Design Handoff export)` above the lock table — the brief names
the export path, node ids and companion schema + generated stamp and never re-describes
the file; lock rows carry only what the file cannot (blend, behaviour, links, copy
overrides); ACs get one **AC-S**, never per-node ACs; the engineer returns a deviation
table (`export path · built value · reason`). Echoed in `agents/engineer.md`,
`skills/capture-figma/SKILL.md` rung 0 (now "a Design Handoff export (Layer Brief)"),
`skills/issue-triage/SKILL.md` and `output-styles/discipline.md`.
**A2 — mechanism named:** a brief states how each locked value is produced — grid
container vs col-span arithmetic, sizing token vs literal, component instance vs inline
markup, which node carries `mix-blend-mode`; "on the grid" without the container is a
steer to arithmetic. `issue-triage` adds "Crop before lane" and "One ask may be two
levers"; `grilling`'s assumption gate adds the direction of a change and the mechanism
that produces a value.
**A3 — the reviewer checks mechanism:** `agents/reviewer.md` gains a Spec-axis
`### Structure check` (a right number by the wrong mechanism is a red finding,
"mechanism mismatch"), reproduces the operator's framing before grading a visual
complaint, proves "pre-existing" against `main`, and never lets a probe share the
build's constant. Look stays the operator's; **file parity** — copy strings, node
presence, tokens, annotations — becomes the reviewer's red-able file check, and
`audit-build` joins the reviewer's stack for UI.
**A4 — slicing stops carrying the first mistake:** every continuation or slice brief
carries `## State (untrusted draft; verify)`, prior-slice choices are claims to
re-verify, one component per sonnet dispatch (`model-routing`), fresh-means-fresh
rebuilds name the export and wiring loci only, the "never edit tests" fence carries the
fixture/golden-path repoint exception, and doer verification servers move to `:3220+`
with `:3210` (operator dev server) and `:3211` (hoverboard viewer) reserved —
`hooks/scripts/workflow-lint.mjs` protects both.
**A5 — lessons ship or say why not:** new `hooks/scripts/lesson-ledger.mjs` lints
`encoded: <semver> | pre-1.73.0 | queued | skipped(<reason>)` on every
`fleet/lessons/*.md` and `fleet/rulings/*.md`;
`hooks/bin/commit-gate.mjs` runs it with `--release` when a staged change bumps
`.claude-plugin/plugin.json`, warn-and-skips when the vault is absent, and is on by default
with `DISCIPLINE_LEDGER_GATE=0` as the opt-out. Index files are exempt by the house
convention (`<folder>/<folder>.md`, `fleet-<folder>.md`, or `kind: index`). `wrap`,
`vault-write` and `skill-authoring` (new `## Lesson intake`) carry the rule.

Dispositions applied by the vault backfill lane (jhd-vault PR, same release).
Lessons encoded in this version: hoverboard rounds 4–7 items 1, 2, 3, 8;
review-evidence-lessons items 1, 2; all four items on
`projects/jhd-discipline/jhd-discipline-handover.md`; and the "mechanism" candidate from
`projects/portfolio/portfolio-handover.md`. Marked `skipped`: hoverboard items 4, 5, 6, 9
(hoverboard-rig-specific) and review-evidence-lessons items 3, 5, 6, 7
(harness-specific).

New law tests: `hooks/scripts/source-contract-law.test.mjs` (28) and
`hooks/scripts/lesson-ledger.test.mjs` (14), plus five commit-gate cases.

1.72.0 — grilling assumption gate, generalised beyond materials (2026-09-09)

Added an assumption gate to `grilling`: before any brief (build or fix round) that
assumes a property the operator could settle in one line — a material or part, a
code convention, a library or approach, an expected behaviour, a content or copy
choice, a data shape, or a naming choice — the parent lists each such property and
asks the operator in one grilling round, the standard ❓/➡️ format, one recommended
answer each. Prompted by the hoverboard 2026-09-08 velcro case, then widened by the
operator: "this could apply to lots of different scenarios not just material
references... but coding things or whatever it may be." `skills/grilling/SKILL.md`
renames the section to "Assumption gate", generalises the rule and the never-coin-
a-term note beyond real-world features, and keeps the domain list to one example
line; description frontmatter generalises the trigger phrases.
`skills/grilling/references/DOS-AND-DONTS.md` keeps the velcro DO/DON'T pair and
adds a matching coding pair under "Assumptions (two examples)".
`skills/dispatch-brief/SKILL.md` checklist and `skills/issue-triage/SKILL.md`'s
part-name section each reword their one line to the general rule.

1.71.0 — retire perplexity-research; settings/permissions guard (2026-09-08)

Removed the `perplexity-research` skill and every reference to it (skill count
46). Research now runs on `research-synthesis` alone (WebSearch/WebFetch) —
no replacement engine. Prompted by tonight's incident: a dispatched Release Ops
agent, blocked on a merge command, tried to rewrite `~/.claude/settings.json`
to widen its own permissions; the harness blocked it. Added a guard rule to
`agents/releaseops.md`, `skills/release-deploy/SKILL.md`, and the scope-fence
section of `skills/dispatch-brief/SKILL.md`: a dispatched agent never edits
settings, permissions, hooks, or plugin config (`~/.claude/**`,
`.claude/settings*.json`); a blocked or denied command is a finding to return
to the parent, never a workaround.

1.70.2 — vocabulary is names of actual things, not only layer names (2026-09-08)

1.70.1 narrowed the canonical vocabulary to "the source file's layer/
component names." The operator corrected this: it doesn't matter whether
the thing is a layer in Illustrator or Figma, or a class or style in code
— the point is consistency in the language and using the names of actual
things. Every thing already has a name where it's defined — a layer,
component, class, token, style, file, or variable — and that name is the
term, whatever the source. `define-terms`, `dispatch-brief`,
`issue-triage`, and `output-styles/discipline.md` now say "names of
actual things wherever defined" instead of "layer names," with the
DO/DON'T examples dropping the layer-only framing; an unmatched term gets
a one-line "which one?" instead of "which layer?" `capture-figma`'s note
is reworded to name its architecture map as one source among several
(design files, code classes/tokens, file names) feeding define-terms,
not the source.

1.70.1 — vocabulary is layer names, not the operator's word (2026-09-08)

1.70.0's part-name fidelity rule framed the canonical term as "the
operator's word." The operator corrected this: the canonical vocabulary is
the layer/component names in the project's source file (Figma,
Illustrator, whatever the source of truth is) — the operator speaks in
those names, and consistency across comms depends on everyone using the
same layer names, not on whoever spoke first. `define-terms` now derives
the glossary from the source file's layer/component list (captured by
`capture-figma` or the project's own capture lane) rather than from
operator usage; an unmatched operator term gets a one-line "which layer?"
question instead of a guess. `dispatch-brief`'s Locked decisions rows and
`issue-triage`'s part-name resolution both quote the layer name verbatim,
with the code identifier (where added) mirroring it. `capture-figma` gains
a short note that its architecture map's layer list is define-terms's
vocabulary source. `output-styles/discipline.md` states the vocabulary is
the source file's names, not a paraphrase.

1.70.0 — pause/resume skill, part-name fidelity (2026-09-08)

New skill `skills/pause-resume/SKILL.md` converts the operator-ratified
pause/resume protocol (`fleet/rulings/pause-resume-protocol.md`) into a
fireable skill with all nine trigger phrases: pause, "I need to pause", "I
need to pause shortly", wind down, stop for now, let's stop here, resume,
pick up where we left off, carry on from the snapshot. Pause halts new
dispatch, stops running agents at their next safe point, banks a one-screen
snapshot into the project handover, and confirms in one line; resume reads
the snapshot and re-dispatches each interrupted lane as a fresh
continuation. Cross-referenced from `routing` (rule 12, persona table) and
the always-on `output-styles/discipline.md` route-by-reflex list, so the
words fire it by reflex instead of falling to `wrap` (which stays the full
session close, not this).

Part-name fidelity: `dispatch-brief`'s Locked decisions section, `define-
terms`, and `issue-triage` now each carry the rule that a physical/visual
part name in the operator's own words travels verbatim — the technical
column may add a code identifier but never substitutes a synonym (ring,
rim, lip, and edge are different parts). `define-terms` adds the part name
to the glossary from the operator's usage before a lane opens, never
guessing it; `issue-triage` quotes the resolved term back to the operator
in one line before a lane opens on a feedback row naming a part. Prompted
by the hoverboard 2026-09-08 miss, where a briefed "ring" cost two lanes
and a review when the operator meant the disc's edge.

  .claude-plugin/marketplace.json
  .claude-plugin/plugin.json
  CHANGED.txt
  output-styles/discipline.md
  skills/define-terms/SKILL.md
  skills/dispatch-brief/SKILL.md
  skills/issue-triage/SKILL.md
  skills/pause-resume/SKILL.md
  skills/routing/SKILL.md

-------------------------------------------------------------------------------
1.69.0 — output style: communication rules first (2026-09-08)

One forced output style. `output-styles/discipline.md` now opens with how a reply
reads, and routing follows it — built from the 2026-09-08 transcript research (58
operator confusion signals across 802 sessions; six named failure types). The rules
are positive and exampled: lead with the outcome; use the vocabulary the work already
has and gloss any system term in the same sentence on first use; a second confusion
signal earns different framing, never the same sentences; walk-backs lead with
"Correction:"; every status ends with one next-action sentence; waiting messages say
what is observably happening; a decision message is one question, one recommendation,
one named alternative; ~120 words is a tripwire, not a rule; a pre-send self-check
closes the section. Borrowed lines are credited in the style.

`operator-voice.md` is retired — its force-for-plugin sibling meant it never fired.
Its load-bearing rules are merged in: announce by invoking the real tool, the four
operator calls (creative, aesthetic, scope, destructive), one recommendation and never
a menu, status against the lock, and all twelve native commands used unprompted
(`/todos /plan /context /compact /code-review /security-review /verify /subtask
/rewind /memory /usage /tasks`) — the law test asserts every one by name. The style is
capped at 130 lines with no tables so it reads on a phone.

`dispatch-brief` gains **Fixed evidence return** — final sha, per-criterion table with
file:line, gate output verbatim, open gaps, next owner, budgeted at 250 words excluding
the table and gate output. Every agent references that one section instead of implying
its own shape. Briefs now name skills rather than restating their procedure, may not
paste vault rulings beyond one DO/DON'T pair, and target under 600 words.

A new law test (hooks/scripts/output-style.test.mjs) asserts the frontmatter flags, the
retirement, each communication rule, and the return contract on a whitespace-flattened
copy, so a reflowed paragraph cannot silently drop a rule. Rules whose words also occur
elsewhere in the file are asserted inside their own paragraph block, so deleting the
block goes red. `node --test hooks/scripts/` reports 455 tests; `node --test hooks/`
reports 461 (it also picks up the hook-level suites outside scripts/).

`.gitignore` now ignores `.ledger.md` — the notes ledger is per-run scratch, not
shipped plugin content.

  .gitignore
  .claude-plugin/marketplace.json
  .claude-plugin/plugin.json
  CHANGED.txt
  agents/engineer.md
  agents/project-manager.md
  agents/releaseops.md
  agents/researcher.md
  agents/reviewer.md
  agents/ux-designer.md
  hooks/scripts/output-style.test.mjs
  output-styles/discipline.md
  output-styles/operator-voice.md
  skills/dispatch-brief/SKILL.md
  skills/grilling/references/DOS-AND-DONTS.md
  skills/skill-authoring/references/DOS-AND-DONTS.md

-------------------------------------------------------------------------------
1.68.0 — review/loop stage redesign (2026-09-07)

The reviewer stops being the merge gate and becomes an informer: severity-ranked
findings (red blocks / amber is a call / note is no action), each independently
re-validated before it surfaces. Merge = deterministic gates green + no red
finding open, remitted by the parent. Review rounds are hard-capped at 3 per
change; at the cap the loop halts and the open findings go to the operator.
LIGHT becomes the standing review tier and FULL the exception a brief must
argue; the no-tier fallback inverts from FULL to LIGHT; small fixes ship with no
reviewer. Deterministic gates must be green before a reviewer is solicited. The
reviewer reads the live lock file and reds a drifted spec. Look is the operator's
lane — for UI work the preview link goes out at engineer-done and never waits on
review. Adds the notes-ledger rule (append before compaction, re-read after) and
an effort tier field (routine | contested | high-stakes) mapped in model-routing.
ux-designer is realigned to the same law — visual evidence is agent-internal, the
baton returns to the parent, and ready is the merge condition rather than a review
verdict. A plugin-wide, newline-tolerant test sweeps every shipped doc for the
stale phrasing.

  .claude-plugin/marketplace.json
  .claude-plugin/plugin.json
  README.md
  agents/engineer.md
  agents/releaseops.md
  agents/reviewer.md
  agents/ux-designer.md
  hooks/scripts/review-loop.test.mjs
  output-styles/discipline.md
  output-styles/operator-voice.md
  skills/dispatch-brief/SKILL.md
  skills/model-routing/SKILL.md
  skills/present-for-review/SKILL.md
  skills/present-for-review/references/DOS-AND-DONTS.md
  skills/quality/SKILL.md
  skills/routing/SKILL.md
  skills/wrap/SKILL.md

-------------------------------------------------------------------------------
1.21.0 — file list below (historical)

.claude-plugin/marketplace.json
.claude-plugin/plugin.json
README.md
agents/project-manager.md
agents/researcher.md
agents/releaseops.md
agents/reviewer.md
agents/ux-designer.md
skills/capture-figma/SKILL.md
skills/capture-website/SKILL.md
skills/define-terms/SKILL.md
skills/design-modules/SKILL.md
skills/design-system/SKILL.md
skills/design-system/references/building-components.md
skills/design-system/references/flux-app.md
skills/design-system/references/portfolio.md
skills/diagnosing-bugs/SKILL.md
skills/discover-scope/SKILL.md
skills/dispatch-brief/SKILL.md
skills/doc-formats/SKILL.md
skills/issue-triage/SKILL.md
skills/model-routing/SKILL.md
skills/motion/SKILL.md
skills/motion/references/BUILD-DOS-AND-DONTS.md
skills/motion/references/REVIEW-DOS-AND-DONTS.md
skills/motion/references/VOCABULARY-DOS-AND-DONTS.md
skills/nextjs/SKILL.md
skills/ops-inbox/SKILL.md
skills/perplexity-research/SKILL.md
skills/prototype/SKILL.md
skills/prototype/references/DOS-AND-DONTS.md
skills/prototype/references/GLOSSARY.md
skills/routing/SKILL.md
skills/shape-stress/SKILL.md
skills/shape-stress/references/DOS-AND-DONTS.md
skills/summarise-meeting/SKILL.md
skills/wrap/SKILL.md

DELETED (merged into siblings at v1.21.0)
skills/animation-vocabulary/
skills/motion-craft/
skills/review-animations/
skills/stress-plan/
