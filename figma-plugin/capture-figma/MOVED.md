# Capture Figma (moved)

This exporter lives in **design-tools**:

`~/JHD/design-tools/capture/figma-sync/`  
(remote: `MrJarrad/design-tools` → `capture/figma-sync/`)

Import in Figma via **Development → Import plugin from manifest…** pointing at that `manifest.json`.

Do not use this discipline path for new work.
